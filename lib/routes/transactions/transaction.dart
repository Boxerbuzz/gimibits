import 'package:flutter/material.dart';

import '../../exports.dart';

class GBTransactions extends StatelessWidget {
  static Screen gmTransactions = Screen(
    title: 'Transactions',
    contentBuilder: (context) => GBTransactions(),
  );

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}